import {useMemo} from 'react';import {useNavigate} from 'react-router-dom';import {Warehouse,Boxes,PackageCheck,ArrowDownToLine,ArrowUpFromLine,ArrowRightLeft,TriangleAlert,PackageX,ArrowUpRight} from 'lucide-react';import {LineChart,Line,XAxis,YAxis,CartesianGrid,Tooltip,Legend,ResponsiveContainer} from 'recharts';import {useApp} from '../context/AppContext';import {warehouseGroups,movementLabels} from '../data/constants';import {fmt,stockStatus} from '../utils/helpers';import {PageHeader,StatCard,StatusBadge} from '../components/common';
const chart=Array.from({length:15},(_,i)=>({day:`${i+1}/7`,รับเข้า:60+(i*17)%95,จ่ายออก:35+(i*23)%80}));
export default function DashboardPage(){const {products,movements}=useApp(),nav=useNavigate();const today=new Date().toISOString().slice(0,10);const stats=useMemo(()=>({stock:products.reduce((a,p)=>a+p.currentStock,0),low:products.filter(p=>stockStatus(p)==='ใกล้หมด').length,out:products.filter(p=>p.currentStock===0).length,receive:movements.filter(m=>m.transactionDate===today&&m.transactionType==='RECEIVE').reduce((a,m)=>a+m.quantityIn,0),issue:movements.filter(m=>m.transactionDate===today&&m.transactionType==='ISSUE').reduce((a,m)=>a+m.quantityOut,0),transfer:movements.filter(m=>m.transactionDate===today&&m.transactionType==='TRANSFER_OUT').reduce((a,m)=>a+m.quantityOut,0)}),[products,movements,today]);const cards=[[Warehouse,'กลุ่มคลังทั้งหมด',5,'คลัง','blue'],[Boxes,'สินค้าทั้งหมด',products.length,'รายการ','purple'],[PackageCheck,'Stock คงเหลือรวม',fmt(stats.stock),'หน่วย','green'],[ArrowDownToLine,'รับเข้าวันนี้',fmt(stats.receive),'หน่วย','green'],[ArrowUpFromLine,'จ่ายออกวันนี้',fmt(stats.issue),'หน่วย','orange'],[ArrowRightLeft,'โอนคลังวันนี้',fmt(stats.transfer),'หน่วย','purple'],[TriangleAlert,'สินค้าใกล้หมด',stats.low,'รายการ','orange'],[PackageX,'สินค้าหมด',stats.out,'รายการ','red']];return <><PageHeader title="ภาพรวมคลังสินค้า" subtitle="ข้อมูลการดำเนินงานล่าสุดของ CSP Foods Supply"/><div className="stats-grid">{cards.map(([I,l,v,u,c])=><StatCard key={l} icon={I} label={l} value={v} unit={u} color={c}/>)}</div><div className="card chart-card"><div className="card-title"><div><h2>แนวโน้มการเคลื่อนไหวสินค้า</h2><p>รับเข้าและจ่ายออก 15 วันล่าสุด</p></div><span className="live-dot">● อัปเดตล่าสุด</span></div><ResponsiveContainer width="100%" height={280}><LineChart data={chart}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="day"/><YAxis/><Tooltip/><Legend/><Line type="monotone" dataKey="รับเข้า" stroke="#16a34a" strokeWidth={3}/><Line type="monotone" dataKey="จ่ายออก" stroke="#f97316" strokeWidth={3}/></LineChart></ResponsiveContainer></div><div className="two-col"><div className="card"><div className="card-title"><h2>สรุปกลุ่มคลัง</h2></div><div className="table-wrap"><table><thead><tr><th>กลุ่มคลัง</th><th>สินค้า</th><th>Stock</th><th>สถานะ</th><th></th></tr></thead><tbody>{warehouseGroups.map(g=>{const ps=products.filter(p=>p.warehouseGroup===g.id),out=ps.filter(p=>p.currentStock===0).length;return <tr key={g.id}><td><b>{g.name}</b></td><td>{ps.length}</td><td>{fmt(ps.reduce((a,p)=>a+p.currentStock,0))}</td><td><StatusBadge status={out?'ใกล้หมด':'ปกติ'}/></td><td><button className="link-btn" onClick={()=>nav(`/warehouse/${g.path}`)}><ArrowUpRight/></button></td></tr>})}</tbody></table></div></div><div className="card"><div className="card-title"><h2>ความเคลื่อนไหวล่าสุด</h2></div><div className="movement-list">{movements.slice(0,7).map(m=><div key={m.id}><span className={`movement-dot ${m.transactionType.toLowerCase()}`}/><div><b>{m.productName}</b><small>{m.documentNo} · {m.warehouseGroup}</small></div><div className={m.quantityIn?'qty-in':'qty-out'}>{m.quantityIn?'+':'-'}{fmt(m.quantityIn||m.quantityOut)}<small>{movementLabels[m.transactionType]}</small></div></div>)}</div></div></div></>}
